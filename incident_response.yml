---
# - name: Incedent Response Info

- hosts: windows
	
  tasks:


- hosts: ubuntu

  tasks:
    - name: Recent Login's
      shell: last -s yesterday
      #Would like to change yesterday to a valuable time frame to look back to
    
    - name: Process Information
      shell: ps -eo pid,command,uid,ppid,fname,pmem,etime
      #Must look further into time, especially for user vs kernel runtime
    
    - name: Suspicious Connections
      shell: ss -tup | grep -v :80 | grep -v :443
